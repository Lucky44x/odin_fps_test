package menustate

MenuGameState :: struct {
    
}

update_menu :: proc(state: ^MenuGameState) {

}

render_menu :: proc(state: ^MenuGameState) {
    render_imgui(state)
}

render_imgui :: proc(state: ^MenuGameState) {
    
}